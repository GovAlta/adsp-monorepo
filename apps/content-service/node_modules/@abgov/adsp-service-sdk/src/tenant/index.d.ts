import { Logger } from 'winston';
import { TokenProvider } from '../access';
import { ServiceDirectory } from '../directory';
import { Tenant, TenantService } from './tenantService';
declare global {
    namespace Express {
        interface Request {
            tenant?: Tenant;
        }
    }
}
export { createTenantHandler } from './tenantHandler';
export type { Tenant, TenantService } from './tenantService';
interface TenantServiceOptions {
    directory: ServiceDirectory;
    tokenProvider: TokenProvider;
    logger: Logger;
}
export declare function createTenantService({ directory, tokenProvider, logger }: TenantServiceOptions): TenantService;
