import type { Request } from 'express';
import type { Logger } from 'winston';
import { IssuerCache } from './issuerCache';
type DoneCallback = (error: Error, result?: string) => void;
type SecretCallback = (req: Request, token: string, done: DoneCallback) => Promise<void>;
export declare class TenantKeyProvider {
    #private;
    private readonly logger;
    private readonly accessServiceUrl;
    private readonly issuerCache;
    private readonly LOG_CONTEXT;
    constructor(logger: Logger, accessServiceUrl: URL, issuerCache: IssuerCache);
    keyRequestHandler: SecretCallback;
}
export {};
