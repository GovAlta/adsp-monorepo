import { Logger } from 'winston';
import type { Tenant, TenantService } from '../tenant';
export declare class IssuerCache {
    #private;
    private readonly logger;
    private readonly accessServiceUrl;
    private readonly service;
    private readonly LOG_CONTEXT;
    constructor(logger: Logger, accessServiceUrl: URL, service: TenantService);
    getTenantByIssuer: (issuer: string) => Promise<Tenant>;
}
