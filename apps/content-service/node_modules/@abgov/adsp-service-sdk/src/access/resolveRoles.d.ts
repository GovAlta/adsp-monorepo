interface Token {
    realm_access?: {
        roles: string[];
    };
    resource_access?: Record<string, {
        roles: string[];
    }>;
}
export declare function resolveRoles(aud: string, { realm_access, resource_access }: Token): string[];
export {};
