import type { Strategy } from 'passport';
import { JwtFromRequestFunction } from 'passport-jwt';
import type { Logger } from 'winston';
import { TenantService } from '../tenant';
import { AdspId } from '../utils';
export interface AccessStrategyOptions {
    realm: string;
    ignoreServiceAud?: boolean;
    serviceId: AdspId;
    tenantService: TenantService;
    accessServiceUrl: URL;
    additionalExtractors?: JwtFromRequestFunction[];
    logger: Logger;
}
export declare const createRealmStrategy: ({ realm, serviceId, tenantService, accessServiceUrl, ignoreServiceAud, additionalExtractors, }: AccessStrategyOptions) => Promise<Strategy>;
