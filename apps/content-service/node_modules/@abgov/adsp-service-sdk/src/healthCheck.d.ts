import { Logger } from 'winston';
import { ServiceDirectory } from './directory';
interface HealthCheckResult {
    directory?: boolean;
    tenant?: boolean;
    access?: boolean;
    configuration?: boolean;
    event?: boolean;
}
/**
 * Performs a health check against platform services.
 *
 * Note that the health check will attempt connections to platform services that are used based on the
 * parameters provided to SDK initialization.
 *
 * @export
 * @interface PlatformHealthCheck
 */
export interface PlatformHealthCheck {
    (): Promise<HealthCheckResult>;
}
export declare const checkServiceHealth: (logger: Logger, healthUrl: URL) => Promise<boolean>;
export declare const createHealthCheck: (logger: Logger, accessServiceUrl: URL, directoryUrl: URL, directory: ServiceDirectory, exclude: Record<string, boolean>) => PlatformHealthCheck;
export {};
