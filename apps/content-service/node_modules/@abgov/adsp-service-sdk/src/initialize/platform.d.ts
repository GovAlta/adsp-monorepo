import type { Logger } from 'winston';
import { LogOptions, PlatformCapabilities, PlatformOptions, PlatformServices } from './types';
export declare function initializePlatform({ realm, serviceId, clientSecret, directoryUrl, accessServiceUrl, ignoreServiceAud, configurationConverter, combineConfiguration, enableConfigurationInvalidation, useLongConfigurationCacheTTL, additionalExtractors, ...registration }: PlatformOptions, logOptions: Logger | LogOptions, service?: Partial<PlatformServices>): Promise<PlatformCapabilities>;
