import { Logger } from 'winston';
import { ServiceRegistrar, ServiceRegistration } from './index';
import { TokenProvider } from '../access';
import { ServiceDirectory } from '../directory';
export declare class ServiceRegistrarImpl implements ServiceRegistrar {
    #private;
    private readonly logger;
    private readonly directory;
    private readonly tokenProvider;
    private readonly LOG_CONTEXT;
    constructor(logger: Logger, directory: ServiceDirectory, tokenProvider: TokenProvider);
    register(registration: ServiceRegistration): Promise<void>;
    private updateConfiguration;
}
