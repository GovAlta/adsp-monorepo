import { Request } from 'express';
import { EndBenchmark } from './types';
export declare function benchmark(req: Request, metric: string, value?: number): void;
export declare function startBenchmark(req: Request, metric: string): EndBenchmark;
