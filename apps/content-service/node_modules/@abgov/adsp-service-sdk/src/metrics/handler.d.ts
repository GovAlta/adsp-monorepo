import { RequestHandler } from 'express';
import { Logger } from 'winston';
import { TokenProvider } from '../access';
import { ServiceDirectory } from '../directory';
import { AdspId } from '../utils';
export declare function writeMetrics(serviceId: AdspId, directory: ServiceDirectory, logger: Logger, tokenProvider: TokenProvider, buffer: Record<string, unknown[]>): Promise<void>;
export declare function createMetricsHandler(serviceId: AdspId, logger: Logger, tokenProvider: TokenProvider, directory: ServiceDirectory, defaultTenantId?: AdspId): Promise<RequestHandler>;
