import { Logger } from 'winston';
import { TokenProvider } from '../access';
import { ServiceDirectory } from '../directory';
import { ConfigurationServiceImpl } from './configurationService';
export declare const handleConfigurationUpdates: (logger: Logger, directory: ServiceDirectory, tokenProvider: TokenProvider, configurationService: ConfigurationServiceImpl) => Promise<void>;
