import type { RequestHandler } from 'express';
import { TokenProvider } from '../access';
import { AdspId } from '../utils';
import type { ConfigurationService } from './configurationService';
export declare const createConfigurationHandler: (tokenProvider: TokenProvider, service: ConfigurationService, serviceId: AdspId) => RequestHandler;
export declare const createTenantConfigurationHandler: (tokenProvider: TokenProvider, service: ConfigurationService, serviceId: AdspId, tenantId: AdspId) => RequestHandler;
