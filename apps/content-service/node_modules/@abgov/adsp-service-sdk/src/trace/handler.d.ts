import { InternalAxiosRequestConfig } from 'axios';
import type { RequestHandler } from 'express';
import type { Logger } from 'winston';
interface TraceHandlerOptions {
    logger: Logger;
    sampleRate: number;
}
export declare function traceRequestInterceptor(config: InternalAxiosRequestConfig): InternalAxiosRequestConfig<any>;
export declare function createTraceHandler({ logger, sampleRate }: TraceHandlerOptions): RequestHandler;
export {};
