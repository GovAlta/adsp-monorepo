import { AIMessage } from '../../lib/types/messages';
export declare const ChatMessage: ({ message, isLoading, }: {
    message: AIMessage;
    isLoading?: boolean;
}) => import("react/jsx-runtime").JSX.Element;
