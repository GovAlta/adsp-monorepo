import { testCreateAuthenticationFlow } from './createAuthenticationFlow';

describe('Tenant:Services:createAuthenticationFlow', () => {
  test('Test stub', async () => {
    expect(testCreateAuthenticationFlow()).toBe(true);
  });
});
