import styled from 'styled-components';

export const FilterFormItemsContainer = styled.div`
  display: flex;
  flex-direction: column;
  padding: var(--goa-space-m) var(--goa-space-l);
  goa-form-item {
    width: 100%;
  }
`;
