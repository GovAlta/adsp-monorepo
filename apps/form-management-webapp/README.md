# form Admin Front End Webapp

The form admin app allows GoA staff to sign-up, provision within the platform.

### Running the service

Start the service using:

```
nx run form-management-webapp:serve
```

### Test locally

```
http://localhost:4200
```
